
import { dateToString } from 'waze-checker/helpers/date-to-string';
import { module, test } from 'qunit';

module('Unit | Helper | date to string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateToString([42]);
  assert.ok(result);
});

